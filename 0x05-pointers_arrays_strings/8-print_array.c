#include "main.h"

/**
 * print_array - check the code
 *
 * @a: param
 * @n: param1
 * Return: Always 0.
 */
void print_array(int *a, int n)
{
	int i = 0;

	while (i < n)
	{
		printf("%d", *(a + i0));

		if (i != n - 1)
		{
			printf(", ");
		}
		i++;
	}
	putchar('\n');
}
